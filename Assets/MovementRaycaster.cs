using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovementRaycaster : MonoBehaviour
{

    private Ray ray;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void FixedUpdate()
    {

        

        
    }

    private void OnDrawGizmos()
    {
        // Draw a ray forward

        // if hit a wall, draw that walls normal from the hit point.

        // draw our initial ray projected flush against the hit surface (wall running).
    }
}
